#lang setup/infotab

(define scribblings '(("carali.scrbl" ())))

(define name "carali")

(define blurb '("CArters RAcket LIbrary - miscellaneous utility package "))
(define primary-file '("main.rkt"))
(define categories '(misc))

(define release-notes
  '((ul (li "Dont really know whats going on"))))

(define homepage "http://www.markcarter.me.uk")
;(define primary-file "carali.rkt")
(define repositories '("4.x"))
(define required-core-version "4.0.0.0")

(define version "0.0")



#|
(define scribblings '(
        ("http.rkt" (multi-page) (carali-library 101))

;("scribblings/carali.scrbl" (multi-page) (carali-library 101))  
                            ))
|#